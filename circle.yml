machine:
  ruby:
    version: 2.3.1

dependencies:
  post:
    - gem install octokit

test:
  override:
    - echo "This is dummy test"

deployment:
  github_release:
    branch: master
    commands:
      - zip test.zip test.txt
      - ruby release.rb
